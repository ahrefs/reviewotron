--- Iteration 1: P0.1 — Add melange-json-native dependency ---
Completed at 2026-04-14 17:58:14
<task-result>
TASK_ID: P0.1
STATUS: DONE
FILES_CHANGED: dune-project, lib/dune, reviewotron.opam
DECISIONS:
- Used melange-json-native v2.0.0 (latest, compatible with dune 3.22.0, ppxlib 0.38.0, OCaml 4.14+)
- Runtime library: `melange-json-native` added to lib/dune libraries
- PPX: `melange-json-native.ppx` added to lib/dune preprocess pps
- Placed alphabetically in dependency lists for consistency
- Did not add to test/dune — tests don't directly use `[@@deriving json]` yet; they get it transitively via `reviewotron_lib`
NOTES:
- melange-json-native 2.0.0 is now installed in the project's opam switch and wired into the build
- All 54 existing tests pass, build succeeds, formatting clean
- The pre-existing AGENTS.md change on `security-review` branch was NOT committed by this task (it was already modified before this iteration)
- Next task (P0.2+) can start using `[@@deriving json]` on OCaml types to replace ATD files
</task-result>

---  P0.2: Migrate slack_types.atd to OCaml types with [@@deriving json] ---
Completed: 2026-04-14 18:16:58
<task-result>
TASK_ID: P0.2
STATUS: DONE
FILES_CHANGED: lib/slack_types.ml (NEW), lib/slack_types.mli (NEW), lib/api.ml, lib/api_remote.ml, lib/review_format.ml, lib/review_format.mli, lib/api_local.ml, lib/dune, lib/slack_types.atd (DELETED)
DECISIONS:
- Used `[@@deriving json]` from melange-json-native on both .ml and .mli files
- .mli uses `[@@deriving json]` to auto-generate val signatures (no manual listing)
- .mli does NOT need `open Melange_json.Primitives` (the PPX generates fully-qualified signatures)
- ATD `~field <default>` mapped to `[@json.default expr]`, ATD `?field` mapped to `[@json.option]`
- Serialization at call sites uses composition: `Melange_json.to_string (X.foo_to_json x)` and `X.foo_of_json (Melange_json.of_string s)` rather than adding `[@@deriving json_string]` — only 2 call sites, not worth a separate deriver
- All references changed from `Slack_types_t.` → `Slack_types.` and `Slack_types_j.` functions → `Slack_types.` + `Melange_json` composition
- Did NOT change pre-existing code patterns outside the migration scope (exception handling, Option.default usage)
NOTES:
- This is the first module using melange-json-native `[@@deriving json]` in the codebase — establishes the pattern for subsequent ATD migrations
- melange-json-native uses `Yojson.Basic.t` while ATD uses `Yojson.Safe.t` — these are distinct types and cannot be mixed in the same expression
- All 54 existing tests pass, build clean, formatter clean
- The `Melange_json.to_string`/`of_string` + `to_json`/`of_json` composition pattern should be used for all future ATD migrations in api_remote.ml
- Next task (P0.3: state.atd migration) can follow the same pattern established here
</task-result>
=== P0.3: Migrate state.atd to OCaml types with [@@deriving json] ===
Completed: 2026-04-14 22:33:03
<task-result>
TASK_ID: P0.3
STATUS: DONE
FILES_CHANGED: lib/state_types.ml (NEW), lib/state_types.mli (NEW), lib/state.ml, lib/state.mli, lib/dune, lib/state.atd (DELETED)
DECISIONS:
- `review_record`, `push_review_record`, and `repo_state` use `[@@deriving json]` from melange-json-native
- `state` type has manual `state_to_json`/`state_of_json` because ATD used `<json repr="object">` for the `repos` field (assoc list serialized as JSON object), which melange-json-native has no annotation for. PRD rule "No manual JSON unless impossible" — this is a genuine impossibility.
- `[@json.default []]` on `pr_reviews` and `push_reviews` replicates ATD's `~field <ocaml default="[]">` behavior
- `.mli` exposes types with `[@@deriving json]` for auto-generated val signatures, plus manual `val state_to_json`/`val state_of_json`
- `save` uses `Yojson.Basic.pretty_to_string` (melange-json-native uses `Yojson.Basic.t`, not `Safe.t`)
- `load` uses `State_types.state_of_json (Melange_json.of_string content)` composition pattern (same as P0.2)
- All `State_t.` references → `State_types.`, all `State_j.` references → `State_types.` + `Melange_json` composition
NOTES:
- The JSON format is backward compatible with existing state files (verified by state_persistence tests passing)
- melange-json-native uses `Yojson.Basic.t` — any future consumer that mixes with ATD types (which use `Yojson.Safe.t`) needs to be aware of this distinction
- Review and simplify passes found no issues specific to this migration; all findings were pre-existing patterns
- All 54 tests pass, build clean, formatter clean
- Next task (P0.4: config.atd migration) can follow this same pattern. Note config.atd may have more complex types — check for `<json repr="object">` and other ATD annotations
</task-result>

=== P0.4: Migrate config.atd to OCaml types with [@@deriving json] ===
Completed: 2026-04-14 23:05:29
<task-result>
TASK_ID: P0.4
STATUS: DONE
FILES_CHANGED: lib/config_types.ml (NEW), lib/config_types.mli (NEW), lib/config.atd (DELETED), lib/config_adapter.ml (DELETED), lib/context.ml, lib/context.mli, lib/api.ml, lib/api_remote.ml, lib/reviewer.ml, lib/github_auth.ml, lib/github_auth.mli, lib/dune, test/test.ml, test/test_helpers.ml
DECISIONS:
- Used `[@@deriving json]` from melange-json-native on `config`, `app_installation_cfg`, `repo_auth`, and `secrets` types
- `repo_config` uses manual `repo_config_of_json`/`repo_config_to_json` because the legacy `gh_token` → `auth` adapter pattern is impossible to express with derived serialization (PRD "no manual JSON unless impossible" — justified)
- melange-json-native variant encoding (`["Name", payload]`) matches ATD `<ocaml repr="classic">` format — backward-compatible with existing secrets.json files
- `config` type has `[@@json.allow_extra_fields]` for forward compatibility (repos may add unknown fields)
- ATD `~field <default>` → `[@json.default expr]`, ATD `?field` → `[@json.option]` (same pattern as P0.2/P0.3)
- Factored out `cons_opt_field` and `nullable_field` helpers to avoid repeated verbose Option matching (per review feedback)
- Used `string_of_json`/`string_to_json` from `Melange_json.Primitives` instead of inline lambdas
- Deleted `config_adapter.ml` — adapter logic inlined into `config_types.ml`'s `normalize_repo_config_json`
- `.mli` does NOT expose `normalize_repo_config_json` or `cons_opt_field`/`nullable_field` (internal helpers)
- All call sites updated: `Config_t.X` → `Config_types.X`, `Config_j.X_of_string s` → `Config_types.X_of_json (Melange_json.of_string s)`
NOTES:
- The `X_of_json (Melange_json.of_string ...)` composition pattern now has 12+ call sites across all migrated types. Consider adding convenience `_of_string`/`_to_string` wrappers as a cross-cutting follow-up for all migrated types (slack_types, state_types, config_types).
- The `test_config_defaults` test exercises the `gh_token` backward-compatibility path (parses `{"gh_token": "tok"}` in repos array)
- melange-json-native uses `Yojson.Basic.t` — any code mixing with ATD types (which use `Yojson.Safe.t`) needs awareness of this distinction
- All 54 tests pass, build clean, formatter clean

=== P0.5: Migrate review_types.atd to OCaml types with [@@deriving json, jsonschema] ===
Completed: 2026-04-14 23:37:35
<task-result>
TASK_ID: P0.5
STATUS: DONE
FILES_CHANGED: lib/review_types.ml (NEW), lib/review_types.mli (NEW), lib/review_types.atd (DELETED), lib/review_prompt.ml, lib/review_prompt.mli, lib/review_format.ml, lib/review_format.mli, lib/reviewer.ml, lib/api.ml, lib/api_remote.ml, lib/api_local.ml, lib/dune, test/test.ml
DECISIONS:
- `severity` and `finding_category` have manual `to_json`/`of_json` because they are open string enums (bare string encoding like `"critical"`, `"error-handling"`) incompatible with melange-json-native's array-based variant encoding (`["Name", payload]`). Justified under PRD "no manual JSON unless impossible."
- Manual `jsonschema` functions for enum types produce `{"type": "string"}` — matches the pre-existing schema behavior in review_prompt.ml
- `finding` and `review_output` use `[@@deriving json, jsonschema]` with `[@@json.allow_extra_fields]` for parsing robustness (matches ATD's default ignore-unknown-fields behavior)
- Extracted `severity_to_string` and `finding_category_to_string` helpers — used by both `_to_json` and `review_format.ml`, eliminating 20+ lines of duplicated variant-to-string mapping
- `review_prompt.ml` duplicate type hierarchy (30 lines) deleted — now references `Review_types.review_output_jsonschema` directly
- Dropped `Ppx_deriving_jsonschema_runtime.json_schema` wrapper (only added `$schema` metadata not needed for Anthropic tool schemas) — uses direct coercion `(review_output_jsonschema :> Yojson.Safe.t)` instead
- `api_remote.ml` uses `Yojson.Safe.to_basic` instead of string round-trip for Safe→Basic conversion
- `.mli` uses explicit val signatures (not `[@@deriving]` in interface) because ppx_deriving_jsonschema doesn't support sig_type_decl
NOTES:
- This is the first module using both `[@@deriving json]` AND `[@@deriving jsonschema]` — establishes the pattern for future security pipeline types
- The `severity_to_string`/`finding_category_to_string` pattern should be reused when implementing security pipeline types (Phase 3+) that also need string-encoded enums (e.g., `vuln_class`, `confidence`)
- `Yojson.Safe.to_basic` works for converting Anthropic API responses (Safe.t) to melange-json-native types (Basic.t) — use this instead of string round-trips in future ATD→melange-json migrations
- `Yojson.Basic.t` can be coerced to `Yojson.Safe.t` via `:>` — no string round-trip needed when going the other direction
- All 54 tests pass, build clean, formatter clean
</task-result>

=== P0.6: Migrate github_types.atd to OCaml types with [@@deriving json] ===
Completed: 2026-04-15 00:09:55
<task-result>
TASK_ID: P0.6
STATUS: DONE
FILES_CHANGED: lib/github_types.ml (NEW), lib/github_types.mli (NEW), lib/github_types.atd (DELETED), lib/github.ml, lib/github.mli, lib/api.ml, lib/api_remote.ml, lib/api_local.ml, lib/reviewer.ml, lib/github_auth.ml, lib/dune, src/request_handler.ml, src/reviewotron.ml
DECISIONS:
- `diff_side` and `review_event` have manual `to_json`/`of_json` because they are bare string enums ("LEFT", "COMMENT") incompatible with melange-json-native's array-based variant encoding. Justified under PRD "no manual JSON unless impossible."
- `create_review_req` uses `[@@deriving of_json]` + manual `create_review_req_to_json` because ATD omits empty default-valued fields (`comments: []`) and the test suite asserts this behavior. `[@json.drop_default]` only works with `[@json.option]` (option types), not lists, so manual serialization is necessary.
- `[@json.option]` handles both ATD's `?field: type nullable` and `?field: type option` patterns correctly — melange-json-native's `option_of_json` maps `Null` → `None`
- `[@json.key "html_url"]` and `[@json.key "ref"]` handle field renames (ATD's `<ocaml name="...">`)
- `[@json.allow_extra_fields]` on all GitHub response types (matching ATD's default ignore-unknown-fields behavior)
- `[@json.option] [@json.drop_default]` on optional request fields (`review_comment_req`, `commit_comment_req`) to match ATD's omit-when-None serialization behavior
- Type aliases `commit_hash` and `pr_action` need `[@@deriving json]` so the PPX can find `commit_hash_of_json`/`commit_hash_to_json` when they appear in derived record types
- `parse_pr_files_json` refactored to parse JSON string once, reuse the JSON tree for both strict and fallback paths (eliminating redundant `Yojson.Basic.from_string` call)
NOTES:
- This is the largest ATD migration (22 types, 13 files changed) — github_types.atd was the most-referenced ATD file in the codebase
- Only `anthropic_types.atd` remains as an ATD file (Phase 1 will replace it with ocaml-ai-sdk)
- The `diff_side_to_string`/`review_event_to_string` pattern reuses the same approach from review_types.ml (severity_to_string/finding_category_to_string). When Phase 3+ adds more string-encoded enums (vuln_class, confidence), consider factoring out a shared `string_enum_to_json`/`string_enum_of_json` helper.
- `Melange_json.of_string` returns `Yojson.Basic.t` — the fallback in `parse_pr_files_json` no longer needs `Yojson.Basic.from_string` since we reuse the already-parsed tree
- All 54 tests pass, build clean, formatter clean
</task-result>

=== P0.7: Remove ATD build dependencies from dune ===
Completed: 2026-04-15 00:17:03
<task-result>
TASK_ID: P0.7
STATUS: DONE
FILES_CHANGED: (none)
DECISIONS:
- No code changes needed — previous migrations (P0.2-P0.6) each removed their own ATD rule from lib/dune when deleting their respective .atd file
- `atdgen-runtime` in lib/dune libraries MUST STAY — anthropic_types generated code depends on it at runtime
- `atdgen` in dune-project dependencies MUST STAY — needed as build-time tool for `%{bin:atdgen}` in the anthropic_types rule
- The anthropic_types.atd rule in lib/dune is the only remaining ATD rule and MUST STAY until Phase 1 (ocaml-ai-sdk integration)
- This task was designed anticipating batch cleanup, but incremental cleanup during each migration made it a verification-only task
NOTES:
- All 54 tests pass, build clean, formatter clean
- Only `lib/anthropic_types.atd` remains as an ATD file in the project
- `Anthropic_types_t` and `Anthropic_types_j` are referenced only in `lib/api_remote.ml` (lines 146-212) — Phase 1 will replace these with ocaml-ai-sdk types
- When Phase 1 deletes anthropic_types.atd, it should also: (1) remove the rule stanza from lib/dune, (2) remove `atdgen-runtime` from lib/dune libraries, (3) remove both `atdgen` and `atdgen-runtime` from dune-project depends, (4) run `dune build` to regenerate the opam file
- Phase 0 is now complete — all non-Anthropic ATD files have been migrated to melange-json-native with [@@deriving json]
</task-result>

=== P1.1: Add ocaml-ai-sdk dependencies ===
Completed: 2026-04-15 00:33:24
<task-result>
TASK_ID: P1.1
STATUS: DONE
FILES_CHANGED: dune-project, lib/dune, reviewotron.opam (auto-generated)
DECISIONS:
- Used `ocaml-ai-sdk` (no version constraint) in dune-project depends — the package is pinned locally from `/home/me/code/opensource/ocaml-ai-sdk` via `opam pin`
- Used public library names in lib/dune (`ocaml-ai-sdk.ai_core`, `ocaml-ai-sdk.ai_provider`, `ocaml-ai-sdk.ai_provider_anthropic`) — internal names (`ai_core`, `ai_provider`, `ai_provider_anthropic`) don't resolve for external packages
- Added all three sub-libraries even though `ai_core` transitively depends on `ai_provider` — explicit is better for documenting direct dependencies
- No changes to `(preprocess ...)` stanza — the SDK doesn't require additional PPX processors beyond what reviewotron already has (`lwt_ppx`, `melange-json-native.ppx`, `ppx_deriving_jsonschema`)
NOTES:
- `opam pin add ocaml-ai-sdk /home/me/code/opensource/ocaml-ai-sdk --no-action -y` was run, then `opam install ocaml-ai-sdk -y`
- 24 transitive packages were installed: cohttp-lwt-unix, lwt_ssl, jsonschema, ppx_deriving, ppx_deriving_yojson, conduit-lwt-unix, ssl, uri, etc.
- The SDK version is 0.2, pinned from local git+file:// source
- All 54 tests pass, build clean, formatter clean
- The SDK uses `cohttp-lwt-unix` for HTTP while reviewotron uses `curl` — both coexist without conflict
- Next task (P1.2) should build the generic `run_agent` function using `Generate_text.generate` from `ai_core`
- The `ai_provider_openai` sub-library is also available but not added (not needed per PRD)
</task-result>

=== P1.2: Implement generic run_agent function ===
Completed: 2026-04-15 00:50:48
<task-result>
TASK_ID: P1.2
STATUS: DONE
FILES_CHANGED: lib/agent_runner.ml (NEW), lib/agent_runner.mli (NEW)
DECISIONS:
- `model_tier` has `[@@deriving json, jsonschema]` in .ml, `[@@deriving json]` + explicit `val model_tier_jsonschema` in .mli (ppx_deriving_jsonschema doesn't support sig_type_decl — same pattern as review_types.ml)
- `agent_config` and `agent_result` are concrete types (not abstract) because callers construct and destructure them directly
- `run_agent` takes `~model:Ai_provider.Language_model.t` directly — provider-agnostic, callers create the model instance with whatever credentials/provider they want
- Tools passed as optional `?tools` parameter (not in `agent_config`) because `Core_tool.t` contains execute callbacks that can't be serialized
- `agent_config` does NOT have `[@@deriving json]` because `output_schema : Yojson.Basic.t` field is incompatible with melange-json-native's ppx (no `Yojson_basic_t_to_json` in scope). Config is constructed in OCaml code, not deserialized.
- Error handling wraps SDK exceptions (`Retry_error`, `Provider_error`) into `(_, string) result` per PRD spec. Local `fail` helper deduplicates the log+return pattern.
- `default_model_id` uses explicit match on all 3 variants (no catch-all) per AGENTS.md
- Uses `Option.default` (Devkit) instead of `Option.value` (stdlib) because `open Devkit` shadows stdlib's `Option`
NOTES:
- All 54 tests pass, build clean, formatter clean
- `agent_result.model_id` stores the model ID string for downstream cost tracking (PRD Section 7.1 `agent_cost.model`)
- `agent_result.usage` is `Ai_provider.Usage.t` with `input_tokens`, `output_tokens`, `total_tokens option` — real values from SDK, not estimates
- The SDK handles multi-step tool loops, retries with exponential backoff, and structured output parsing internally — `run_agent` is a thin wrapper
- Next task (P1.3) should refactor `Api.Claude` into `Api.Agent_runner` with the new signature, using this module
- When P1.3 rewrites the general review to use `run_agent`, it will need to create the Anthropic model with `Ai_provider_anthropic.language_model ~api_key:secrets.anthropic_api_key ~model:config.model ()` (not the convenience `Ai_provider_anthropic.model` which reads from env var)

=== P1.3: Refactor Api.Claude into Api.Agent_runner ===
Completed: 2026-04-15 01:23:14
<task-result>
TASK_ID: P1.3
STATUS: DONE
FILES_CHANGED: lib/api.ml, lib/api_remote.ml, lib/reviewer.ml, lib/reviewer.mli, lib/api_local.ml, lib/dune, lib/anthropic_types.atd (DELETED), dune-project, reviewotron.opam, src/request_handler.ml, test/test.ml, test/test_helpers.ml
DECISIONS:
- `Api.Claude` renamed to `Api.Agent_runner` with generic signature: `run ~ctx ~repo_url ?model_id ~config ~input () -> (agent_result, string) result Lwt.t`
- `?model_id:string` optional parameter allows repo config model override; falls back to `Agent_runner.default_model_id config.model_tier` when omitted
- `Api_remote.Agent_runner.run` creates `Ai_provider_anthropic.language_model` from `secrets.anthropic_api_key` and resolved `model_id`
- `reviewer.ml` gained `run_review_agent` helper that builds agent_config (`name="general_review"`, `model_tier=Standard`, `output_schema=review_output_jsonschema`, `max_steps=1`), calls `AI.run`, and parses the structured output JSON back to `review_output`
- `Api_local.Agent_runner` mock returns raw JSON as `agent_result.output` with dummy `Usage.t { input_tokens=0; output_tokens=0; total_tokens=None }` — the reviewer now handles JSON→review_output parsing uniformly
- `repo_url` kept in `Api.Agent_runner.run` signature (unused now, needed for future per-repo model routing and security memory); typed `ignore` in implementation
- `anthropic_types.atd` deleted along with its dune rule; `atdgen` and `atdgen-runtime` removed from dune-project depends and lib/dune libraries — ATD is fully gone from the project
- Used `Option.default` combinator for model_id fallback per AGENTS.md style (simplified during review)
- `claude_response_path`/`set_claude_response_path`/`reset_claude_response_path` renamed to `agent_response_path`/`set_agent_response_path`/`reset_agent_response_path` in api_local.ml
NOTES:
- All 54 tests pass, build clean, formatter clean
- `model_tier` in the general review agent_config is effectively overridden by `~model_id:config.model` from repo config — this is by design for Phase 1 (security agents in P3+ will use tier-based resolution without override)
- `secrets.anthropic_version` field is now dead code — the SDK handles API versioning internally. Cleanup candidate for a future task.
- `Review_prompt.estimate_prompt_tokens` is no longer called in production code (SDK tracks real usage) but is still tested. Could be removed if not needed.
- Duplicated default model ID string `"claude-sonnet-4-5-20250929"` exists in both `config_types.ml` (`[@json.default]`) and `agent_runner.ml` (`default_model_id`). Can't easily deduplicate because `[@json.default]` requires a literal. Cross-reference comment would help.

=== P1.4: Delete anthropic_types.atd and manual HTTP code ===
Completed: 2026-04-15 01:39:27
<task-result>
TASK_ID: P1.4
STATUS: DONE
FILES_CHANGED: lib/config_types.ml, lib/config_types.mli, test/test.ml, test/test_helpers.ml, secrets.json.example
DECISIONS:
- P1.3 already completed the bulk of P1.4's planned work (deleting anthropic_types.atd, removing atdgen rules/deps, removing build_anthropic_request and HTTP code). P1.4 focused on the remaining cleanup.
- Removed dead `anthropic_version` field from `secrets` type — the SDK handles API versioning internally, so this field had no consumers in production code
- Added `[@@json.allow_extra_fields]` to the `secrets` type in config_types.ml so existing secrets JSON files containing the removed field still parse correctly (backward compatibility)
- Updated `secrets.json.example` to remove the dead field from the user-facing example
- Added a backward-compatibility regression test (`test_config_ignores_removed_fields`) that parses JSON containing the removed `anthropic_version` field and verifies it succeeds — guards against accidental removal of `[@@json.allow_extra_fields]`
NOTES:
- All 55 tests pass (32 reviewotron + 23 diff_parser), build clean, formatter clean
- ATD is now completely gone from the project — no .atd files, no atdgen deps, no Anthropic_types_t/Anthropic_types_j references in source code
- `secrets.anthropic_version` references remain only in historical plan docs (phase-1-skeleton.md, phase-3-claude-engine.md, phase-6-deployment.md) and progress notes — these are historical records, not code
- `Review_prompt.estimate_prompt_tokens` is still in the codebase and tested but unused in production (SDK tracks real usage). It could be removed in a future cleanup task but is not part of P1.4's scope.
- Phase 1 step 5 ("Delete anthropic_types.atd, manual HTTP code, build_anthropic_request") from the PRD is now fully complete across P1.3 + P1.4
</task-result>

=== P2.1: Define Review_plugin module type ===
Completed: 2026-04-15 01:55:29
<task-result>
TASK_ID: P2.1
STATUS: DONE
FILES_CHANGED: lib/review_plugin.ml (NEW), lib/review_plugin.mli (NEW)
DECISIONS:
- Module type named `S` inside `Review_plugin` module (idiomatic OCaml convention for the primary signature in a dedicated module) — consumers write `Review_plugin.S`
- `review_metadata` type defined alongside the module type in `review_plugin.ml` (not in `review_types.ml`) because it's part of the plugin interface contract, not an agent output type
- `review_metadata` includes `pr_number : int` in addition to `pr_title`, `pr_description`, and `file_contents` — plugins need this for logging and cost tracking
- `file_contents` kept as `(string * string) list` (path, content pairs) to match the existing codebase pattern used by `fetch_key_files` and `format_file_content`
- No `[@@deriving json]` on `review_metadata` — the type is constructed in OCaml code and passed in-memory, never serialized. Can be added later if tests need it.
- `val run` returns `Review_types.finding list Lwt.t` per PRD spec (not `result`) — plugins handle errors internally and return empty list on failure
- All parameters use labeled arguments per AGENTS.md (>2 params rule)
NOTES:
- All 55 tests pass (23 diff_parser + 32 reviewotron), build clean, formatter clean
- The `.mli` has full doc comments on the module, the type, every record field, and both `val` declarations
- P2.2 (wrap general review as plugin) will construct `review_metadata` in the orchestrator and pass it to the plugin's `run` function
- The `file_contents` field is designed for the general review plugin; the security plugin (P3+) will ignore it and fetch files on-demand via tools
- No changes to `lib/dune` needed — the new files are automatically included in the `reviewotron_lib` library
</task-result>

=== P2.2: Wrap general review in General_review_plugin ===
Completed: 2026-04-15 02:11:33
<task-result>
TASK_ID: P2.2
STATUS: DONE
FILES_CHANGED: lib/general_review_plugin.ml (NEW), lib/general_review_plugin.mli (NEW), lib/reviewer.ml
DECISIONS:
- `General_review_plugin` is a functor `Make(AI : Api.Agent_runner)` producing a module that satisfies `Review_plugin.S` plus an extended `run_review` function returning full `Review_types.review_output`
- `name = "general"` matching the config key `review_plugins.general` from the PRD
- `run_review` is the core function (moved from `reviewer.ml`'s `run_review_agent`); `run` wraps it, extracting just findings and returning `[]` on error per PRD plugin contract
- `run` accepts `~diff:_` (unused) because `Review_plugin.S` requires it; the general review uses raw diff text, not parsed file diffs
- In `reviewer.ml`, `module General_plugin = General_review_plugin.Make(AI)` instantiates the plugin inside the `Reviewer.Make` functor; both `execute_and_post_review` and `review_push` construct `Review_plugin.review_metadata` and call `General_plugin.run_review`
- `reviewer.ml` calls `run_review` (not `run`) to get the full `review_output` including `summary` and `overall_assessment` — preserves existing behavior for PR review body and Slack attachment formatting
- Push review path uses `pr_number = 0` as sentinel (pre-existing design from P2.1's `review_metadata` type)
NOTES:
- All 55 tests pass (23 diff_parser + 32 reviewotron), build clean, formatter clean
- `run_review_agent` is fully deleted from `reviewer.ml` — all review agent logic now lives in `General_review_plugin`
- The `.mli` documents that `run_review` is a transitional API for use while the full plugin pipeline is being built (P2.4)
- When P2.4 builds the orchestrator, it will call plugins through the `Review_plugin.S` view (using `run`), and the summary/overall_assessment handling in `execute_and_post_review` will need redesign
- No changes to `lib/dune` needed — new files auto-included in `reviewotron_lib`
- No changes to `reviewer.mli` — external interface (`process_event`) unchanged
</task-result>

=== P2.3: Extend config with review_plugins section ===
Completed: 2026-04-15 02:27:51
<task-result>
TASK_ID: P2.3
STATUS: DONE
FILES_CHANGED: lib/config_types.ml, lib/config_types.mli, test/test.ml
DECISIONS:
- Defined `vuln_class`, `confidence`, and `model_tier` enum types in `config_types.ml` with custom string-based JSON serialization (lowercase strings like `"injection"`, `"fast"`, `"medium"`) suitable for human-authored `.reviewotron.json` config files
- `model_tier` is structurally identical to `Agent_runner.model_tier` but defined separately to keep `config_types` as a leaf module with no dependencies on agent_runner — the dependency direction (agent code depends on config, not vice versa) is preserved
- All plugin config record types (`general_plugin_config`, `security_plugin_config`, `review_plugins_config`) use `[@@deriving json] [@@json.allow_extra_fields]` with `[@json.default]` on every field, so existing configs without `review_plugins` continue to parse correctly
- Default values (`default_general_plugin_config`, `default_security_plugin_config`, `default_review_plugins_config`) are explicit record literals rather than JSON round-trips, making them grep-friendly and independent of the serialization layer
- `all_vuln_classes` convenience value is exposed for use as default and for iteration
- Added `show_review_cost : bool` with `[@json.default false]` to the `config` type per PRD section 5.1
- 4 new tests: defaults verification, explicit config parsing, vuln_class roundtrip, security_plugin_config roundtrip
NOTES:
- Total test count: 59 (23 diff_parser + 36 reviewotron), all passing
- P3.1 (security pipeline types) will need to decide how to handle `vuln_class` and `confidence` which now exist in `config_types` — options: (a) re-export from config_types, (b) add `[@@deriving jsonschema]` to the config_types versions since security pipeline types need JSON Schema for structured output, or (c) define separate types in security_types with a conversion function
- A `Config_types.model_tier → Agent_runner.model_tier` mapping function will be needed when the security plugin constructs agent configs (Phase 3+) — trivial to add at that point
- String enum serialization pattern (to_string → to_json, of_json with string matching) is repeated 3 times here and also exists for `severity` and `finding_category` in review_types.ml — a shared `string_enum_to_json`/`string_enum_of_json` combinator could DRY this up in a future cleanup pass
- `ppx_enumerate` could eliminate the manual `all_vuln_classes` list but the ppx isn't in the dune config — adding it is out of scope for this task
</task-result>

=== P2.4: Build plugin orchestrator in reviewer.ml ===
Completed: 2026-04-15 02:43:59
<task-result>
TASK_ID: P2.4
STATUS: DONE
FILES_CHANGED: lib/reviewer.ml
DECISIONS:
- Plugin orchestrator implemented as `run_plugins` function inside the `Make` functor — runs enabled plugins, collects findings, merges, deduplicates, returns `(review_output option, finding list)`
- General plugin is run through `run_review` (not `Review_plugin.S.run`) because PR body/Slack formatting needs the full `review_output` (summary + overall_assessment), not just findings
- Deduplication key is `(path, line, category)` — keeps the finding with higher severity rank when duplicates are found; results sorted by (path, line) for deterministic output
- `severity_rank` function maps severity variants to numeric ranks (Critical=5, Warning=4, ... Other=0) for dedup comparison
- `run_plugins` does NOT take `~diff` parameter (YAGNI — will be added in Phase 3 when security plugin needs parsed file diffs)
- `execute_and_post_review` accepts `~config` from caller instead of re-fetching via `Context.get_config`
- Used `Stdlib.Option.compare Int.compare` instead of polymorphic `compare` for type-safe line number comparison (Devkit shadows stdlib's `Option.compare`)
- Used `Option.default` from Devkit (not `Option.value`) because `open Devkit` shadows stdlib
NOTES:
- All 59 tests pass (23 diff_parser + 36 reviewotron), build clean, formatter clean
- Only lib/reviewer.ml changed — no interface changes to reviewer.mli, no new files
- When the general plugin is disabled via config and no other plugins are enabled, both PR and push paths log an error and return early ("no review output produced")
- The `run_plugins` return type `(review_output option * finding list)` means findings exist both inside the review_output and as a separate deduplicated list — callers should use the deduplicated list for comments. This design will benefit from a cleaner abstraction when Phase 3 adds the second plugin
- The duplicated `match general_result | None -> error | Some -> proceed` pattern in both `execute_and_post_review` and `review_push` is a known duplication; deferring cleanup to Phase 3 when the return type can be redesigned
- `severity_rank` lives in reviewer.ml for now; could be promoted to Review_types in a future cleanup pass

=== P3.1: Define security pipeline types ===
Completed: 2026-04-15 03:00:16
<task-result>
TASK_ID: P3.1
STATUS: DONE
FILES_CHANGED: lib/security_types.ml (NEW), lib/security_types.mli (NEW), test/test.ml
DECISIONS:
- `vuln_class` and `confidence` are re-exported from `Config_types` via type equality syntax (`type vuln_class = Config_types.vuln_class = ...`) preserving structural compatibility between modules
- `to_json`/`of_json`/`to_string` for enums delegated to Config_types (no duplication); `jsonschema` defined locally as `{ "type": "string" }` matching the pattern in `review_types.ml`
- All record types use `[@@deriving json, jsonschema]` for automatic serialization and schema generation
- `sanitization_status` and `validation_verdict` (variants with payloads) use derived serialization — melange-json-native's array-tag format (`["Inadequate", "reason"]`, `["Rejected", "reason"]`)
- `[@@json.allow_extra_fields]` applied to `triage_output`, `candidate_finding`, `analysis_output`, `validator_output` — the types that are LLM structured output targets (extra fields from LLM are common and harmless)
- `[@@json.option]` on `skip_reason` and `suggested_fix` option fields so they're omitted from JSON when None
- `source_evidence`, `sink_evidence`, `flow_step` kept as separate types despite identical structure — PRD specifies them separately, and distinct types provide safety (prevents accidentally swapping source for sink)
- `@jsonschema.description` annotations on variant constructors omitted — verified that ppx_deriving_jsonschema does not support constructor-level descriptions (no tests or implementation for it in the ppx source)
- 7 roundtrip tests covering: triage_output (with and without skip_reason), candidate_finding (all fields), sanitization_status (all 4 variants with exact JSON verification), validation_verdict (both variants), validator_output (nested structure), extra fields tolerance
NOTES:
- Total test count: 66 (23 diff_parser + 43 reviewotron), all passing, build clean, formatter clean
- `to_string` functions also re-exported for convenience — downstream code (logging, prompt generation, routing) will need them without reaching through to Config_types
- No changes to `lib/dune` needed — new files auto-included in `reviewotron_lib`
- P3.2 (get_file_content tool) will likely need to import `Security_types` for agent output schemas
- P3.3 (triage agent config) will use `triage_output_jsonschema` as the structured output schema and `vuln_class`/`confidence` constructors for routing logic

=== P3.2: Implement get_file_content tool ===
Completed: 2026-04-15 03:16:28
<task-result>
TASK_ID: P3.2
STATUS: DONE
FILES_CHANGED: lib/security_tools.ml (NEW), lib/security_tools.mli (NEW), test/test.ml
DECISIONS:
- Tool factory pattern: `make_get_file_content ~fetch_file` takes a callback closure `(string -> (string option, string) result Lwt.t)` and returns `(string * Ai_core.Core_tool.t)` — a named tool pair ready for `Agent_runner.run_agent ~tools`
- The `fetch_file` callback abstracts away `ctx`, `repo_url`, and `ref_` — the security plugin will capture those in a closure when constructing the tool, keeping the tool module decoupled from API types
- Three-state return contract: `Ok (Some content)` = success, `Ok None` = not found, `Error msg` = API error — each maps to a typed `get_file_content_result` with `content` and `error` option fields
- Param parsing errors from malformed JSON are caught via `Melange_json.Of_json_error (Json_error msg | Unexpected_variant msg)` — exhaustive over the error type's variants — and returned as structured error results rather than exceptions
- `[@json.option]` on result fields means missing JSON keys parse as `None` (defensive for LLM responses) and `None` values serialize as `null` (not omitted)
- `[@jsonschema.description]` annotations on all fields provide context in the JSON Schema exposed to Claude agents
- `[@@deriving json]` in `.mli` and `val jsonschema : Yojson.Basic.t` in `.mli` — `ppx_deriving_jsonschema` does not support `.mli` derivation, matching the pattern in `review_types.mli` and `security_types.mli`
- 10 new tests: 5 serialization (params roundtrip, result with content/error/empty, missing fields parsing), 4 execute integration (success, not found, API error, invalid params), 1 structural (tool name)
NOTES:
- Total test count: 76 (23 diff_parser + 53 reviewotron), all passing, build clean, formatter clean
- No changes to `lib/dune` needed — new files auto-included in `reviewotron_lib`
- The `Core_tool.execute_tool` in the SDK catches exceptions from the execute callback and wraps them as `is_error = true` tool results — so even if `fetch_file` raises, the agent loop continues safely
- P3.3 (triage agent config) will construct the tool via `Security_tools.make_get_file_content ~fetch_file:(fun path -> Github.get_file_content ~ctx ~repo_url ~path ~ref_ ())` or similar
- P3.4 (security plugin skeleton) will pass the tool list to `Agent_runner.run_agent ~tools:["get_file_content", tool]`
- `open Melange_json.Primitives` in `.ml` is required for ppx-generated code (`string_of_json`, etc.) — same pattern as `review_types.ml` and `security_types.ml`

=== P3.3: Build triage agent config and prompt ===
Completed: 2026-04-15 03:32:37
<task-result>
TASK_ID: P3.3
STATUS: DONE
FILES_CHANGED: lib/triage_agent.ml (NEW), lib/triage_agent.mli (NEW), test/test.ml
DECISIONS:
- Module structure follows `review_prompt.ml` pattern: static system prompt + `config` function + `build_input` function for user message assembly
- `config ~model_tier` requires explicit model tier from caller (no default) — forces the security plugin to make an explicit decision from `security_plugin_config.triage_model_tier`, which is the cleaner design
- `detect_languages` exposed in `.mli` for reuse by future analysis agents (they need language hints from triage output, but this utility is also useful for constructing their inputs)
- `language_of_extension` kept private — implementation detail, not useful outside this module
- `build_input` injects pre-computed language hints into the user message AND the system prompt asks the LLM to produce `language_hints` in output — intentional dual path: extension-based hints give the LLM context, LLM's output may detect additional languages from diff content (e.g. embedded SQL)
- System prompt biases toward over-flagging per PRD 4.1: "cheap to spawn an analysis agent that finds nothing, costly to skip one that would have found something"
- `max_steps = 1` (single-shot, no tools) per PRD 4.1
- Output schema is `Security_types.triage_output_jsonschema` (derived from types, no manual JSON)
- Used `Printf.bprintf` for direct buffer writes (cleaner than `Buffer.add_string` + `Printf.sprintf`)
- Empty security memory (`Some ""`) treated same as `None` — consistent with existing pattern in `review_prompt.ml`
- 9 new tests: config defaults, model tier passthrough (all 3 tiers), detect_languages (normal/empty/unknown+unrecognized extensions), build_input (minimal/with memory/empty memory), output schema structure validation
NOTES:
- Total test count: 85 (23 diff_parser + 62 reviewotron), all passing, build clean, formatter clean
- No changes to `lib/dune` needed — new files auto-included in `reviewotron_lib`
- P3.4 (security plugin skeleton) will call `Triage_agent.config ~model_tier:security_config.triage_model_tier` and `Triage_agent.build_input ~diff_text ~file_paths ?security_memory ()`, then pass the result to `AI.run ~config ~input`

=== P3.4: Build security plugin skeleton with triage routing ===
Completed: 2026-04-15 04:05:04
<task-result>
TASK_ID: P3.4
STATUS: DONE
FILES_CHANGED: lib/security_review_plugin.ml (NEW), lib/security_review_plugin.mli (NEW), lib/reviewer.ml, test/test.ml
DECISIONS:
- Functor signature `Make (_ : Api.Github) (AI : Api.Agent_runner)` — GH parameter included for future Phase 4 analysis agents (currently unused, suppressed with `_`)
- Two-gate routing logic per PRD: `confidence >= threshold` triggers unconditionally, below threshold only triggers if `vuln_class` is in configured `vuln_classes` list — implemented as `||` in `should_analyze`
- `vuln_class_equal` uses exhaustive pattern match (not string comparison) so compiler warns when new variants are added — follows AGENTS.md "avoid catch-alls for refactorability"
- `agent_model_tier` conversion function added since `Config_types.model_tier` and `Agent_runner.model_tier` are structurally identical but separate types (intentional decoupling per Config_types docs)
- `confidence_rank`, `vuln_class_equal`, `should_analyze`, `agent_model_tier` exposed in `.mli` for direct unit testing — pragmatic choice over testing only through the functor
- Plugins run concurrently via `Lwt.both` in `run_plugins` — general and security plugins are independent, concurrent execution nearly halves wall-clock time
- `run_plugins` gained a `~diff` parameter (parsed file diffs) needed by the security plugin for file path extraction; both callers (`execute_and_post_review` and `review_push`) updated
- Analysis agents are stubbed — `run_analysis` logs which agents would be spawned and returns empty findings; Phase 4 will replace this
- 9 new tests covering: confidence_rank ordering, vuln_class_equal (same/different/all-self), should_analyze (6 scenarios: above threshold, below in config, below not in config, high threshold, high threshold restricted, low threshold), agent_model_tier conversion
NOTES:
- Total test count: 94 (23 diff_parser + 71 reviewotron), all passing, build clean, formatter clean
- No changes to `lib/dune` needed — new files auto-included in `reviewotron_lib`
- The `review_push` path previously ignored parsed diff (`_filtered_diff`) — now uses it to pass `~diff:filtered_diff` to `run_plugins`
- The `Api.Agent_runner` interface does not currently support `~tools` parameter — Phase 4 will need to extend it for analysis agents that use `get_file_content`
- `Lwt.both` in `run_plugins` was identified by efficiency review as the key optimization; the previous sequential approach left the security plugin waiting for the general plugin to finish

=== P3.5: Test triage against initial corpus diffs ===
Completed: 2026-04-15 04:32:03
<task-result>
TASK_ID: P3.5
STATUS: DONE
FILES_CHANGED: test/security_corpus/injection/sql_concat_vulnerable.diff (NEW), test/security_corpus/injection/sql_parameterized_safe.diff (NEW), test/mock_api_responses/triage/injection_vulnerable.json (NEW), test/mock_api_responses/triage/safe_no_signals.json (NEW), test/dune, test/test.ml
DECISIONS:
- Corpus diffs use Python (Flask) as the target language — realistic SQL injection patterns, and Python is well-supported by the triage agent's language detection (.py → "Python")
- Vulnerable diff shows SQL string concatenation with `request.args.get('id')` as the source and `db.execute(query)` as the sink — clear injection pattern
- Safe diff shows parameterized queries with `db.execute(query, (user_id,))` — same file structure as vulnerable diff for controlled comparison
- Mock triage JSON uses lowercase variant names (`"injection"`, `"high"`) matching melange-json-native's `[@@deriving json]` serialization format
- Tests are infrastructure-level (deterministic, no Claude calls), not LLM quality tests — corpus diffs serve dual purpose: now for pipeline testing, later (Phase 7) for agent prompt quality testing
- Extracted `parse_single_diff` and `parse_triage_file` helpers to eliminate duplicate code across tests (identified during simplify review)
- Used pattern matching on single-element lists `| [ x ] -> ... | _ -> fail ...` instead of banned `List.hd` per AGENTS.md style guide
- 7 tests cover: diff validity (2), build_input assembly (1), triage response parsing (2), routing logic (1), safe skip (1)
NOTES:
- Total test count: 101 (23 diff_parser + 78 reviewotron), all passing, build clean, formatter clean
- The `language_hints` in mock JSON uses lowercase `"python"` (LLM output format), while `detect_languages` produces capitalized `"Python"` — both are valid, as `language_hints` comes from the LLM, not from `detect_languages`
- The corpus structure follows PRD 8.2: `test/security_corpus/injection/` with vulnerable and safe variants
- Phase 7 will expand the corpus with XSS, command injection, authn, authz, and SSRF categories
- `test/dune` deps updated with `security_corpus/injection/*` and `mock_api_responses/triage/*` globs
</task-result>

=== P4.1: Build shared analysis agent framework ===
Completed: 2026-04-15 05:04:23
<task-result>
TASK_ID: P4.1
STATUS: DONE
FILES_CHANGED: lib/analysis_agent.ml (NEW), lib/analysis_agent.mli (NEW), lib/api.ml, lib/api_remote.ml, lib/api_local.ml, test/test.ml
DECISIONS:
- Module follows `Triage_agent` pattern: `config` + `build_input` + `tools` functions
- `config ~vuln_class ~model_tier ~language_hints` requires all params explicitly — no defaults, forces caller to make conscious choices from security plugin config
- `max_steps = 10` for analysis agents (multi-step tool loop for demand-driven context expansion via `get_file_content`)
- Per-class prompt sections extracted as named string constants (`injection_section`, `xss_section`, etc.) with exhaustive match dispatch in `vuln_class_section` — compiler warns when new variants are added
- `build_system_prompt` uses `String.concat "\n\n"` joining preamble + methodology + class section + tools/output instructions — cleaner than imperative Buffer for static structure
- `build_input` uses `Buffer` (dynamic structure with conditional sections) — consistent with `Triage_agent.build_input` pattern
- Language hints appended as a note to per-class sections via string concatenation (not `Printf.sprintf` format strings, since named constants are `string` not `format`)
- Agent name pattern: `"security_analysis_{vuln_class}"` for logging clarity
- Output schema: `Security_types.analysis_output_jsonschema` (PPX-derived, no manual JSON)
- Extended `Api.Agent_runner` module type with `?tools:(string * Ai_core.Core_tool.t) list` parameter — minimal change, backward compatible (optional param)
- `api_remote.ml` passes `?tools` through to `Agent_runner.run_agent`; `api_local.ml` accepts and ignores tools in mock
- `append_regions` writes directly into caller's buffer (avoids intermediate allocation identified in efficiency review)
- 13 new tests: config defaults, per-class naming, model tier passthrough, prompt methodology presence, per-class section presence, language hint parameterization, build_input (minimal/with memory/empty memory), tools, output schema, shared methodology, vuln_class_section coverage
NOTES:
- Total test count: 91 (23 diff_parser + 68 reviewotron), all passing, build clean, formatter clean

=== P4.2: Implement injection analysis agent prompt ===
Completed: 2026-04-15 05:20:36
<task-result>
TASK_ID: P4.2
STATUS: DONE
FILES_CHANGED: lib/analysis_agent.ml
DECISIONS:
- Expanded `injection_section` in-place rather than creating a separate prompt file — keeps all per-class sections colocated with the dispatch logic in `vuln_class_section`
- Structured prompt with 4 sections mirroring shared_methodology's 4 steps: Sources, Sinks, Sanitization Assessment, False Positive Patterns
- Sources catalog covers OCaml/Dream, JavaScript/Express, Python/Django/Flask with specific API entry points
- Sinks organized by injection subtype (SQL, NoSQL, ORM) with per-language examples
- Added "Common False Positive Patterns — DO NOT REPORT" section to reduce noise (Caqti type-safe binding, Django ORM queryset methods, Sequelize model methods, etc.)
- Removed LDAP injection and SSTI from scope statement per review — no concrete sink catalogs for these, and SSTI is a distinct vuln class
- Added second-order injection awareness to scope statement
- Preserved "SQL/Query Injection" substring in title for test compatibility (test at test.ml:744)
- Prompt grows from ~150 to ~900 tokens — modest increase justified by language-specific precision
NOTES:
- Total test count: 114 (23 diff_parser + 91 reviewotron), all passing, build clean, formatter clean
- The other 5 sections (xss, command_injection, authn, authz, ssrf) remain as short placeholders — tasks P4.3-P4.7 will expand them to match this structure
- No .mli changes needed — `injection_section` is internal, accessed only through the exposed `vuln_class_section`
- No new tests needed — prompt quality is tested by corpus in Phase 7; infrastructure tests already validate the wiring
</task-result>

--- Iteration 21: P4.3 — UNCLEAR RESULT ---
2026-04-15 05:26:54

=== P4.3: Implement XSS analysis agent prompt ===
Completed: 2026-04-15 05:36:54
<task-result>
TASK_ID: P4.3
STATUS: DONE
FILES_CHANGED: lib/analysis_agent.ml
DECISIONS:
- Expanded `xss_section` from 4-line placeholder to ~100-line comprehensive prompt following the same structure as `injection_section`
- Organized sources into two categories: Server-Side Sources (reflected + stored XSS) and Client-Side Sources (DOM-based XSS) — clearer than three separate subsections per XSS type
- Sources catalog covers OCaml/Dream, JavaScript/Express, Python/Django/Flask for server-side; canonical DOM sources (location, hash, postMessage, window.name, Web Storage) for client-side
- Sinks organized into four groups: Server-Side Template Rendering, Client-Side DOM Manipulation, Client-Side JavaScript Execution, Framework-Specific Dangerous APIs
- Framework coverage: React (dangerouslySetInnerHTML), Vue (v-html), Angular ([innerHTML]/bypassSecurityTrustHtml), Svelte ({@html}), jQuery (.html/.append/$)
- Sanitization assessment: adequate patterns (context-correct encoding, DOMPurify, framework auto-escaping, allowlist sanitizers, CSP as defense-in-depth) and inadequate patterns (wrong-context encoding, blocklist filtering, regex stripping, client-only validation, strip_tags, truncation, encodeURIComponent alone)
- False positive patterns include: React JSX auto-escaping, Django/Jinja2 auto-escaping, Tyxml type safety, JSON/plain-text responses, static HTML, textContent/innerText
- Review fix 1: Moved `render_template_string` from Django bullet to Flask/Jinja2 bullet (it's a Flask function, not Django)
- Review fix 2: Corrected `encodeURIComponent` description — it does encode `<>"` to percent-encoding, but the issue is it produces percent-encoding rather than HTML entities, and doesn't protect in unquoted attributes or event handler contexts
NOTES:
- Total test count: 91 (23 diff_parser + 68 reviewotron), all passing, build clean, formatter clean
- The previous iteration (21) completed this work but was marked UNCLEAR — this iteration verified, reviewed, and fixed two minor accuracy issues
- The other 4 sections (command_injection, authn, authz, ssrf) remain as short placeholders — tasks P4.4-P4.7 will expand them
- No .mli changes needed — `xss_section` is internal, accessed only through the exposed `vuln_class_section`
- Prompt grows from ~150 to ~950 tokens — modest increase justified by language-specific precision and false positive reduction

--- Iteration 23: P4.4 — UNCLEAR RESULT ---
2026-04-15 05:47:20

=== P4.4: Implement command injection analysis agent prompt ===
Completed: 2026-04-15 06:09:20
<task-result>
TASK_ID: P4.4
STATUS: DONE
FILES_CHANGED: lib/analysis_agent.ml
DECISIONS:
- Expanded `command_injection_section` from 4-line placeholder to ~80-line comprehensive prompt following the same 4-section structure as injection_section and xss_section (Sources, Sinks, Sanitization Assessment, False Positive Patterns)
- Key structural distinction: split sinks into "Shell-Mediated Execution" (highest risk, shell metacharacters interpreted) vs "Direct Process Execution" (no shell, but prog/args may be user-controlled) — this is the most important conceptual framework for command injection analysis
- Added "Argument injection" special-case subsection covering dangerous flags in programs like git (--upload-pack), tar (--checkpoint-action), curl (-o) — often missed category
- Sources catalog covers OCaml/Dream, JavaScript/Express, Python/Django/Flask, plus cross-language sources (second-order via DB, file paths, env vars, message queues)
- Sanitization: adequate patterns include array-form exec, shlex.quote (Unix), Filename.quote (Unix), allowlist, library APIs, `--` separator; inadequate patterns include blocklist filtering, Filename.quote on Windows (detailed Win32 quoting issues), complex pipeline edge cases, wrong-context encoding, truncation
- False positive patterns: hardcoded commands, array-form with safe programs, allowlist-validated enum selection, build/test-only code, log/debug string construction
- Review fix 1: Added `Unix.open_process` (bidirectional variant) to complete the OCaml shell-mediated sinks family alongside open_process_in/out/full
- Review fix 2: Added `subprocess.call` and `subprocess.check_call` list-form variants to Python direct execution sinks
- Simplify fix: Added `subprocess.check_output` list-form to Python direct execution sinks for completeness
NOTES:
- Total test count: 91 (23 diff_parser + 68 reviewotron), all passing, build clean, formatter clean
- The remaining 3 sections (authn, authz, ssrf) are still short placeholders — tasks P4.5-P4.7 will expand them
- No .mli changes needed — `command_injection_section` is internal, accessed only through the exposed `vuln_class_section`
- Prompt grows from ~150 to ~1000 tokens — consistent with injection (~900) and XSS (~950) sections
</task-result>

=== P4.5: Implement AuthN analysis agent prompt ===
Completed: 2026-04-15 06:17:17
<task-result>
TASK_ID: P4.5
STATUS: DONE
FILES_CHANGED: lib/analysis_agent.ml
DECISIONS:
- Expanded `authn_section` from 15-line placeholder to ~95-line comprehensive prompt following the same 5-section structure as injection, XSS, and command injection sections (intro paragraph, Sources, Sinks, Sanitization Assessment, False Positive Patterns)
- Opening paragraph explicitly calls out that AuthN vulnerabilities differ from data-flow vulns: they often manifest as **missing or incorrect security logic** rather than dangerous data flows. Instructs the agent to look for absent security steps alongside the standard source→sink methodology
- Sources section renamed from "(User-Controlled Input)" to "(Authentication Material)" — more accurate for AuthN context; bridge text explains these are "credentials, tokens, and session identifiers that enter the authentication boundary"
- Sinks organized into 5 subtypes: Token/JWT Verification, Password Comparison and Storage, Session Management, Auth Middleware/Decorators (with concrete code-pattern examples per framework), and OAuth Flow Validation
- Auth Middleware subsection includes concrete examples per framework (Dream route without middleware, Express router without passport, Django view without decorators, DRF ViewSet with AllowAny) — addresses review feedback that abstract "look for missing middleware" is too vague for LLMs
- Added OAuth subsection covering state parameter validation (CSRF), redirect_uri allowlisting, and authorization code session binding — addresses review feedback that OAuth-specific attacks were missing from original placeholder
- Removed specific bcrypt work factor threshold (`>= 10`) from Adequate section to avoid contradiction with False Positive section's "work factor tuning is a configuration choice" — replaced with "appropriate work factors"
- Framework coverage: OCaml/Dream + Jose library, JavaScript/Express + passport + jsonwebtoken + express-session, Python/Django + Flask + PyJWT + werkzeug + itsdangerous
NOTES:
- Total test count: 91 (23 diff_parser + 68 reviewotron), all passing, build clean, formatter clean
- The remaining 2 sections (authz, ssrf) are still short placeholders — tasks P4.6-P4.7 will expand them
- No .mli changes needed — `authn_section` is internal, accessed only through the exposed `vuln_class_section`
- Prompt grows from ~150 tokens to ~1000 tokens — consistent with injection (~900), XSS (~950), command injection (~1000) sections
- Code review, simplify review (3 agents: reuse, quality, efficiency), and quality gates all passed cleanly
</task-result>

=== P4.6: Implement AuthZ analysis agent prompt ===
Completed: 2026-04-15 06:33:03
<task-result>
TASK_ID: P4.6
STATUS: DONE
FILES_CHANGED: lib/analysis_agent.ml
DECISIONS:
- Expanded `authz_section` from 15-line placeholder to ~90-line comprehensive prompt following the same 5-section structure as injection, XSS, command injection, and AuthN sections (intro paragraph, Sources, Sinks, Sanitization Assessment, False Positive Patterns)
- Opening paragraph explicitly distinguishes AuthZ from AuthN: "who are you?" vs "are you allowed to do this?" — and notes that AuthZ vulnerabilities manifest as missing checks where the "sanitization" is the authorization check itself
- Sources section renamed to "(Resource Identifiers and Permission Context)" to reflect that AuthZ sources are resource IDs combined with the user's identity/role claims, not just raw user input
- Sinks organized into 5 subtypes: Resource Access by Identifier (IDOR Risk), Data Mutation Operations, Administrative/Privileged Operations, Bulk/Cross-Tenant Operations, Mass Assignment — richer taxonomy than other sections, justified by the nature of authorization vulnerabilities
- Mass Assignment subsection covers JavaScript (Mongoose findByIdAndUpdate with unfiltered req.body), Python (DRF serializer with privilege fields, unfiltered create), and OCaml (deserializing JSON into record with role field)
- Framework coverage: OCaml/Dream (including Dream.session_field for role claims), JavaScript/Express (including req.user from passport, x-tenant-id headers), Python/Django/DRF/Flask/SQLAlchemy/Flask-Login
- Review fix 1: Removed incorrect `Dream.field` reference — `Dream.field` is a request-level typed variable accessor, not for multipart forms
- Review fix 2: Replaced incorrect Mongoose `select: false` claim (that's a read-side control, not mass assignment defense) with correct pattern: destructure/pick allowed fields from req.body
- Review fix 3: Corrected DRF `queryset` class attribute description from "only used for schema generation" to "used for router registration and schema generation"
- Review fix 4: Made OCaml mass assignment bullet more concrete with `user_of_yojson body_json` example
- Added UUID-as-sole-defense to Inadequate patterns per plan requirement for IDOR via predictable IDs
NOTES:
- Total test count: 91 (23 diff_parser + 68 reviewotron), all passing, build clean, formatter clean
- The remaining 1 section (ssrf) is still a short placeholder — task P4.7 will expand it
- No .mli changes needed — `authz_section` is internal, accessed only through the exposed `vuln_class_section`

